import 'package:flutter/material.dart';

class TeamTransferes extends StatelessWidget {

  final Key key;
  TeamTransferes(this.key) : super(key: key);


  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('transferes'),),
    );
  }
}