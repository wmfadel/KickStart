import 'package:flutter/material.dart';

class TeamDetails extends StatelessWidget {
  
  static final String routeName = 'teamDetails';
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Team Details screen'),),
    );
  }
}
